import React from "react";
import { ScrollView, View, Text } from "react-native";

export default function AddUser({ setAppState }) {
  return (
    <ScrollView>
      <View>
        <Text>Hello!</Text>
      </View>
    </ScrollView>
  );
}
